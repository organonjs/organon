import { RulesConfig } from "@commitlint/types";
declare const config: {
    parserPreset: string;
    rules: RulesConfig<import("@commitlint/types").RuleConfigQuality.User>;
};
export default config;
